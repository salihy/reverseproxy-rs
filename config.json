{
    "routes": [
        {
            "upstream_path": "/:name",
            "downstream_path": "/hello/:name",
            "downstream_method": "GET",
            "downstream_uri": "localhost:7979"
        }
    ]
}